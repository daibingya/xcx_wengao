<!--pages/manDetails/manDetails.wxml-->
<view class="manDetailsBox" wx:if="{{showFlag}}">
  <view class="title">{{contData.title}}</view>
  <view class="content">
        {{contData.content}}
  </view>
  <button open-type="share" class="shera" data-title="{{contData.title}}" data-sid="{{contData.id}}">分享</button>
</view>
